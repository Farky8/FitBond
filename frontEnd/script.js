const form = document.querySelector('creation')

form.addEventListener('submit', (event) => {
    event.preventDefault();
    const data = new FormData(event.target);

    //TODO make a json and send it
})